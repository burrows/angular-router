(function() {

describe('$router', function() {
  it('should foo', function() {
    expect(1).toBe(1);
  });

  it('should bar', function() {
    expect(1).toBe(1);
  });

  it('should baz', function() {
    expect(1).toBe(1);
  });
});

}());

